plugins {
    id 'java'
    id 'base'
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

jar {
    manifest {
        attributes 'Main-Class': 'jbody.Simulator'
        archivesBaseName = 'qwer'
    }
}

test {
    testLogging {
        events "PASSED", "STARTED", "FAILED", "SKIPPED"
        exceptionFormat 'full'
    }
}

group = 'net.sfedu'
version = '1.0-SNAPSHOT'